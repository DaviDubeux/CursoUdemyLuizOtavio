/* 

\ escapa o caractere

'string'[index]
 
.charAt(index) -> letra

.concat('', '', '') -> +

.indexOf('palavra') -> busca o primeiro índice que contem a 'palavra' (se encontrar)

.lastIndexOf('palavra') -> similar ao .indexOf, mas de trás pra frente

.match(/a-z/g) -> cria uma array com a expressão regular

.search('string' ou expresão regular) similar ao indexOf, mas aceita expressões

.replace('string1', 'string2') substitui a primeira string1 por uma string2, se colocar como 
/string1/g substitui todas as strings1

.length() -> indica o tamanho da string

.slice(index1, index2) -> recorta a string nos índices indicados

.substring() igual ao slice só que pior

.split(' ') -> recorta a string em strings menores nos intervalos indicados no parêntese

.toUpperCase() -> deixa tudo em maiúsculo

.tuLowerCasae() -> deixa tudo em minúsculo
*/
