const num1 = window.prompt('Digite o primeiro número da soma');
const num2 = window.prompt('Digitei o segundo número da soma');

const resultado = (parseInt(num1) + parseInt(num2));

window.alert(`O resultado da soma é ${resultado}`)